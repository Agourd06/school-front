import React from 'react';
import PdfLayoutSettings from '../../components/settings/PdfLayoutSettings';

const PdfLayoutSettingsPage: React.FC = () => {
  return <PdfLayoutSettings />;
};

export default PdfLayoutSettingsPage;
